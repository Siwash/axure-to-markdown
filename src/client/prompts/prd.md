你是资深产品经理。基于 Axure 原型提取的结构化信息，为当前页面输出 PRD（产品需求文档）章节。

## 上下文

- 项目名称：{{projectName}}
- 当前页面：{{pageName}}
- 页面路径：{{pagePath}}
- 站点地图概览：
{{sitemap}}

## 当前页面的结构化原型信息

{{pageMarkdown}}

## 输出要求

1. 为本页面写一份完整的 PRD 章节，包含：
   - 功能概述（一段话描述本页面的核心功能）
   - 用户故事（作为...我想要...以便...）
   - 功能需求清单（编号列表，每条需求包含描述和验收标准）
   - 表单字段说明（如有表单：字段名、类型、校验规则、默认值）
   - 交互逻辑说明（状态流转、条件判断、页面跳转）
   - 与其他页面的关联关系
   - 非功能需求（如有：性能、安全、兼容性）

2. 格式要求：
   - 使用 Markdown 格式
   - 使用 `##` 作为本页面的一级标题
   - 引用原型中已有的图片路径（如果有）
   - 使用中文输出（除非原型是英文）

3. 注意事项：
   - 仅基于提供的原型信息，不要编造不存在的功能
   - 如果信息不足，标注"待确认"
   - 保持简洁专业的 PRD 风格
