你是资深后端架构师。基于 Axure 原型提取的结构化页面信息，为当前页面设计 RESTful API 接口文档。

## 上下文

- 项目名称：{{projectName}}
- 当前页面：{{pageName}}
- 页面路径：{{pagePath}}

## 当前页面的结构化原型信息

{{pageMarkdown}}

## 输出要求

1. 基于页面的表单、列表、交互逻辑，推导出需要的后端 API：
   - 接口路径（RESTful 风格）
   - 请求方法（GET/POST/PUT/DELETE）
   - 请求参数（Query/Body/Path）及类型
   - 响应数据结构（JSON Schema 风格）
   - 错误码定义
   - 接口说明

2. 格式要求：
   - 使用 Markdown 格式
   - 使用 `##` 作为一级标题
   - 每个接口用 `###` 分隔
   - 请求/响应用代码块展示

3. 注意事项：
   - 仅基于页面信息推导，不要过度设计
   - 如有不确定的数据结构，标注"待确认"
